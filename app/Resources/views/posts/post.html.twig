{% extends 'base.html.twig' %}

{% block body %}

    {#<div class="jumbotron col-sm-12">#}
    {#<blockquote>#}
    {#<p>{{ post.title }}</p>#}
    {#<small>{{ post.description }}</small>#}
    {#</blockquote>#}
    {#</div>#}
    {#<div class="col-sm-8 col-sm-offset-2">#}
    {##}
    {#</div>#}

    <div class="col-sm-10 col-sm-offset-1">
        <div class="col-sm-8 col-sm-offset-2">
            <img src="{{ vich_uploader_asset(post.image, 'imageFile') }}" width="100%" alt="{{ post.image.imageName }}">
        </div>
        <div class="col-sm-8">
            <h1>{{ post.title }}</h1>
            <p>{{ post.description }}</p>
        </div>

        <div class="col-sm-12">
            <hr>
        </div>
    </div>
    <div class="col-sm-8 col-sm-offset-2">
        <p>{{ post.content }}</p>
    </div>



{% endblock %}

{% block title %}

{% endblock %}

